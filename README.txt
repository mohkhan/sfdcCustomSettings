Getting Started

At the command prompt

Go to script root
run 'bundle install'

Configure your source and destination credentials at credentials.yml

For fetching run
'ruby script.rb fetch'

For loading run
'ruby script.rb load'

Note: Since Loading runs in background job check Bulk Data Load Jobs for results